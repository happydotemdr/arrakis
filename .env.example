# Database connection string (PostgreSQL) - OPTIMIZED FOR NEON
# Replace with your actual database URL from Render
# The optimized version includes PgBouncer pooling parameters for better performance and cost reduction
DATABASE_URL="postgresql://username:password@host:port/database?sslmode=require&pgbouncer=true&pool_mode=transaction&connection_limit=20&idle_timeout=30&connect_timeout=10"
# Direct URL for migrations (should NOT use pooling)
DIRECT_URL="postgresql://username:password@host:port/database?sslmode=require"

# Application configuration
NODE_ENV="development"
PORT="3000"

# Next.js configuration
NEXTAUTH_SECRET="your-nextauth-secret-here"
NEXTAUTH_URL="http://localhost:3000"

# OpenAI API Key (if using AI features)
OPENAI_API_KEY="your-openai-api-key-here"